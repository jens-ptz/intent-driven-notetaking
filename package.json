{
  "name": "intent-driven-notetaking",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d db",
    "migrate": "dotenv -e .env -- pnpm --filter @notes/api exec prisma migrate deploy",
    "seed": "dotenv -e .env -- pnpm --filter @notes/api run seed",
    "dev": "dotenv -e .env -- pnpm --parallel --filter @notes/api --filter @notes/web run dev",
    "build": "pnpm --filter @notes/shared run build && pnpm --recursive run build",
    "typecheck": "pnpm --recursive run typecheck",
    "test:acceptance": "pnpm --filter @notes/acceptance-tests run test",
    "lint:specs": "pnpm --filter @notes/acceptance-tests run lint:specs"
  },
  "devDependencies": {
    "typescript": "5.7.3",
    "dotenv-cli": "^8.0.0"
  }
}
